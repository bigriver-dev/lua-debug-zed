{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Lua Debug Configuration",
  "type": "object",
  "required": ["request"],
  "properties": {
    "request": {
      "type": "string",
      "enum": ["launch", "attach"],
      "description": "Request type of this launch configuration"
    },
    "program": {
      "type": "string",
      "description": "Absolute path to the Lua file to debug"
    },
    "stopOnEntry": {
      "type": "boolean",
      "default": false,
      "description": "Automatically stop after launch"
    },
    "cwd": {
      "type": "string",
      "description": "Working directory"
    },
    "args": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Command line arguments"
    },
    "luaVersion": {
      "type": "string",
      "enum": ["5.1", "5.2", "5.3", "5.4", "5.5", "luajit"],
      "default": "5.3",
      "description": "Which Lua version's lua-dap-server build to launch. Downloaded automatically from GitHub Releases on first use, per platform."
    },
    "preloadPaths": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Folders whose .lua files and .dll/.so C modules are automatically loaded into the global table before the debug target runs. .dll/.so files must export a luaopen_<filename> entry point (the standard Lua C-module convention)."
    }
  }
}
